@layout MainLayout
@page "/messaging"
@using Dexterity.Components.Pages.Messaging.Tabs
<TopTitleBar Title="Messaging"
             Image="modules/messaging/communication1.svg">
</TopTitleBar>

<div class="vertical-space">
    <InnerLayout>
        <NavigationItems>
            <DexMenuItem Text="User Tasks" Icon="fas fa-bars-progress" Click="@(() => ClickChanged(0))"/>
            <DexMenuItem Text="Contacts" Icon="fas fa-address-book" Click="@(() => ClickChanged(1))"/>
            <DexMenuItem Text="SMS Messaging" Icon="fas fa-inbox" Click="@(() => ClickChanged(2))"/>
            <DexMenuItem Text="Instant Messaging" Icon="fas fa-message" Click="@(() => ClickChanged(3))"/>
            <DexMenuItem Text="Phone Directory" Icon="fas fa-phone-rotary" Click="@(() => ClickChanged(4))"/>
        </NavigationItems>

        <DexContentTabs >
                <MudTabs @ref="_tabs">
                    <MudTabPanel Text="" Style="background-color: transparent">
                        <UserTasksTab/>
                    </MudTabPanel>
                    <MudTabPanel Text="">

                    </MudTabPanel>
                    <MudTabPanel Text="">
                        <SmsTab/>
                    </MudTabPanel>
                    <MudTabPanel Text="">

                    </MudTabPanel>
                    <MudTabPanel Text="">

                    </MudTabPanel>
                    <MudTabPanel Text="">

                    </MudTabPanel>

                </MudTabs>
        </DexContentTabs>
    </InnerLayout>
</div>

@code {
    MudTabs _tabs;

    private void ClickChanged(int tabIndex)
    {
        _tabs.ActivatePanel(tabIndex);
    }


}